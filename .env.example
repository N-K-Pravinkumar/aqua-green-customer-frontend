# URL of the backend API (see ../backend). Leave unset for local dev
# (defaults to http://localhost:8080/api). Set this in your hosting
# provider's dashboard (Vercel/Netlify/Render) for production.
REACT_APP_API_URL=https://your-backend.onrender.com/api
